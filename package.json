{
  "name": "@escalated-dev/plugin-community",
  "version": "0.1.0",
  "description": "Public community forums with categories, topics, voting, and ticket conversion — TypeScript SDK version",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@escalated-dev/plugin-sdk": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
